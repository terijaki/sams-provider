name: Bump events package version

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    branches: [main]
    paths:
      - "packages/sams-provider-events/src/constants.ts"
      - "packages/sams-provider-events/src/schemas.ts"
      - "packages/sams-provider-events/src/types.ts"

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: bump-events-package-${{ github.event.pull_request.number }}
  cancel-in-progress: false

jobs:
  bump:
    name: Bump contract version
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: packages/sams-provider-events
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0

      - name: Skip if version already bumped above main
        id: check
        run: |
          git fetch origin main
          if git cat-file -e "origin/main:packages/sams-provider-events/package.json" 2>/dev/null; then
            MAIN_VERSION=$(git show origin/main:packages/sams-provider-events/package.json | node -pe "JSON.parse(require('fs').readFileSync(0)).version")
          else
            MAIN_VERSION="0.0.0"
          fi
          HEAD_VERSION=$(node -pe "require('./package.json').version")
          echo "main_version=$MAIN_VERSION" >> "$GITHUB_OUTPUT"
          echo "head_version=$HEAD_VERSION" >> "$GITHUB_OUTPUT"
          node -e "
            const main = process.argv[1].split('.').map(Number);
            const head = process.argv[2].split('.').map(Number);
            const cmp = (a, b) => {
              for (let i = 0; i < 3; i++) {
                if (a[i] !== b[i]) return a[i] - b[i];
              }
              return 0;
            };
            process.stdout.write(cmp(head, main) > 0 ? 'already_bumped' : 'needs_bump');
          " "$MAIN_VERSION" "$HEAD_VERSION" > /tmp/bump-state.txt
          echo "state=$(cat /tmp/bump-state.txt)" >> "$GITHUB_OUTPUT"

      - name: Bump patch version
        if: steps.check.outputs.state == 'needs_bump'
        run: |
          npm version patch --no-git-tag-version
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add package.json
          git commit -m "chore(release): bump sams-provider-events version"
          git push origin HEAD:${{ github.head_ref }}
