Issues are tracked in GitHub Issues. Public consumers request production event delivery with the **Register as a consumer** template; everything else uses **General issue**.

This repository is the SAMS provider; consumer migration issues live in the consumer app repositories and stay blocked until provider events are stable.
